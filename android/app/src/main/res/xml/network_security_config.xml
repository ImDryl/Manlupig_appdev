<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Dev: allow HTTP to Symfony (Lexik json_login). Stricter config recommended for release. -->
    <base-config cleartextTrafficPermitted="true">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
</network-security-config>
